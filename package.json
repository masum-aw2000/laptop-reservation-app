{
  "name": "item-reservation-system",
  "version": "0.0.0",
  "type": "module", 
"scripts": {
  "start": "concurrently --names 'SERVER,CLIENT' \"npm run server\" \"npm run client\"",
  "server": "nodemon server.js",
  "client": "npm run dev --prefix frontend"
}
,
  "dependencies": {
    "express": "^4.18.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "concurrently": "^7.0.0",
    "nodemon": "^2.0.20"
  }
}
